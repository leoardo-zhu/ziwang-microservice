server:
  port: 6868


eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://47.101.39.237:${server.port}/eureka/

spring:
  application:
    name: ziwang-eureka